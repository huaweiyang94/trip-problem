Steps to build and run the app:

1. Run the build.sh script to build the project
2. After build success, run the app with run.sh script
3. A file output.dat will be generated in the root folder
4. You should be able to see all the results wrote in output.dat